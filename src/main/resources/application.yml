spring:
  datasource:
    url: jdbc:mariadb://192.168.182.99:3306/bom
    driver-class-name: org.mariadb.jdbc.Driver
    username: ysh
    password: qwer1234
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace